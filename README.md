# go-webview2 — Semantic module

Imported from `github.com/jchv/go-webview2` at pseudo-version `v0.0.0-20260205173254-56598839c808` (upstream commit `56598839c808a2340edee99204db479f410e9bf4`).

This package contains the original Go source under `source/`, Semantic projections under `semantic/`, package metadata in `go-webview2.smod`, and upstream license material under `licenses/`.

The Semantic projections are imported artifacts. This repository does not claim that every transitive dependency has been imported or that the package has been validated as a standalone executable.

Upstream project: https://github.com/jchv/go-webview2
